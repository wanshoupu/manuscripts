%%
%% Author: shoupu_wan
%% 7/14/18
%%

\chapter{Construct Lean Loops} \label{ch:loops}

As one of the most widely used building block, loop is a programing productivity amplifier---with
a static body and some nominal overheads (\textit{e.g.}, loop variables,
which are used to register loop state), a loop can be executed as many times as needed.
Almost all major programming languages feature one or more loop constructs.
``What's the point to talk about loops as a type of control flow outside of
the language context?'' you may be wondering.


A key building block for programming as loop is, it should have taken
developers' best effort to study the best practices.
It is unfortunate to see, however, that algorithmic implementations in production contain numerous
haphazard loops with floppy code before and after.
Many of them seem to just narrowly pass the `working' threshold
as if the developers were oblivious with the varieties of loop constructs or bother to
reduce the swollen initialization for their loop constructs.
%Have you deliberately tried out a few loop constructs and chosen the best fit for a specific
%application?
%I wish your answer to these questions are both positive.
This is so not because these developers have no appetite for better code.
A majority of them are not even aware that conscious choice of loop types and structures can make a
marked difference in the quality of code.


For this reason that I would like to devote this chapter to clear out the clouds over the
good practices of loops.
A large part of this chapter is on the topic ``How to tighten up slack loops?''
Another focus is on the question ``How to reduce nested loops to a single loop?''
A third one is ``How to avoid inadvertent namespace contamination due to use of loop that have
been the causes of many treacherous bugs?''


For completeness, a summary of
later discussions of the interactions between conditional statements and loop is built on
top of reader's mastery of loop programming.
As in ~\fullref{ch:organizeYourIfElse}, most of the techniques presented in this chapter are
first-hand, based on the author's own coding experience.
But most importantly, I intend this chapter can inspire enterprising
programmers to habitually reflect on their code and discover new ways to improve.


\section{Loop anatomy}\label{sec:loopAnatomy}

\begin{table}[htb]
    \begin{center}
        \caption{Loop anatomy\label{tbl:loop-construct}}
        \begin{tabular}{l|p{10cm}}
            \hline
            Component & Description \\
            \hline
            \emph{Loop initialization} &
            This is the preparation section before the execution of a loop.
            For \code{while}-loop, this is done outside of the loop.
            In \code{fore}-loop, this may be done in the \code{<init>} section of the
            \code{for}-loop construct.\\
            \emph{Loop test} &
            The condition for control to enter (or continue) the loop. \\
            \emph{Loop body} &
            The block of statements to be executed in the loop. \\
            \emph{Loop state update statements} & Almost all loops come with stateful loop
            variables.
            Updating the loop variables are important job of the loop.\\
            \emph{\code{break} statement} &
            A statement in loop body that breaks the loop and pass control to the next statement
            following the loop. \\
            \emph{\code{continue} statement} &
            A statement in loop body that interrupt the execution of the remaining
            statements in the loop.
            Note that this doesn't break the loop or pass control out of the loop. \\
            \emph{\code{return}, \code{yield}, \textit{etc.}} & These statements are
            not part of the loop construct yet they affect the control flow of the loop. \\
            \emph{Post-loop processing} &
            These are rare loop constructs, such as \code{while..else} and \code{for..else}
            in \python. \\
            \hline
        \end{tabular}
    \end{center}
\end{table}


Let us begin with an overview of loop constructs from several mainstream programming languages.
First off, one should ensure that control have a way to escape before entering a loop, otherwise
inadvertent infinite loop would result.
So all loop constructs are equipped with a conditional expression, a termination test.
The location of termination test is a distinguishing feature for a loop.
Based on its location, loop constructs can be classified as \emph{pre-test}, \emph{post-test},
and somewhere in between, \emph{mid-test}.
post-test loops attempt to execute the loop body at least once no matter what.
Mid-test loops may execute part of the loop body.


Many programming languages conveniently support more than one types of loop constructs.
\code{for}-loop and \code{while}-loop are examples of pre-test loop constructs that
almost all languages support.
\code{do-while}-loop is a post-test loop construct supported by \java, \cpp, C, and many
others (excluding \python).


~\autoref{tbl:loop-construct} summarizes the common parts in loop constructs of most high-level
programming languages.
On top of the standard loop constructs, possible customizations may be made with \code{break},
\code{continue}, \code{return}, \textit{etc.}, depending on the programming language of choice.
These statements blend into the loop body and fine tune the execution paths of otherwise standard
loops.


Let us illustrate the abstract concepts with concrete examples.
Say, in a cooking contest, each chef is to cook 3 dishes in a course---appetizer, main,
and desert.
Interested judges will approach the team and put an order of a full course.
Other than judge orders, chef does not cook anything else.
The competition process may be best coded with pre-test loop such as
\lstinputlisting{loop/pre_test_loop.java}


Suppose, on the last night of the competition, the rule is changed so that each chef is to cook a
course for display before judges' orders.
One can do a `copy-n-paste' to solve the problem;
or one can avoid `copy-n-paste' with a post-test loop:
\lstinputlisting{loop/post_test_loop.java}


For another example, suppose we are to develop a command line tool that takes input from
\code{stdin}.
The tool interprets user input and does a bunch of cool stuff.
It terminates when user types `exit' or simply hits `return' key
(in which case the input string is empty).
Then the best loop construct for that would be mid-test loop.
But since \java does not have a specialized mid-test loop construct, we have to makeshift by using
a \code{break} statement along with an infinite loop:
\lstinputlisting{loop/mid_test_loop.java}


\section{A word on the usage of \code{continue}, \code{break}, and \code{return}}
\label{sec:continue-and-break}

We have touched the topic in ~\fullref{sec:branchinginloop} of ~\fullref{ch:organizeYourIfElse}.
For most cases, where \code{continue} statement is used may be
equivalently realized by adding an \code{else}-clause.
But when used in a `cascading conditional' (see ~\fullref{ch:conditional-cascade}),
`\code{continue}' can simplify the loop body like a magic.
Earlier in ~\fullref{ch:organizeYourIfElse}, we illustrated the use of \code{continue} statement in
`Zoo animal health exams' problem.
At the risk of repetition, I am going to provide yet another example if only this can inspire the
reader to strive for optimal loop construction.
Suppose we are coding a game for two or more players with the following rules:
\begin{enumerate}
    \item All players take turn to enumerate natural numbers starting from $1$;
    \item When the number is divisible by $2$, the player write the number down then claps their
    hands;
    \item Otherwise when the number is divisible by $3$, the player write the number down then
    stump their feet;
    \item Otherwise when the number is divisible by $5$, the player do nothing;
    \item Otherwise the player write down the number and articulate the number loudly.
\end{enumerate}
These rules are applied in the order as they appear.
Namely, a `$10$' is not be skipped;
neither is `$15$';
but `$25$' is.

How would one solve the problem?
Without using `\code{continue}', I see two choices---either use composite tests such as
`\code{n \% 5 and not n \% 2 and not n \% 3}; or use nested conditionals somewhere.
Interested readers may try this on their own.
For the first choice, it involves redundant tests.
Suppose an additional rule regarding `$7$' is added, then the places to change would be doubled.
For the second choice, the readability would be compromised with the `switching
smell'\cite{Fowler1999}.
Even worse, duplicate code may chip in by repeatedly invoking `\code{result.append(i)}' for the
cases of `$2$' and `$3$', respectively.


Thankfully, an implementation using `\code{continue}' statement in conjunction
with a cascade conditional is free from any of the problems listed above:
\lstinputlisting{loop/use_of_continue.py}
It is marvelous!

%TODO settle down this presentation
%% side-by-side two columns
%\begin{clisting}
%    \begin{minipage}[t]{.45\textwidth}
%        \lstinputlisting[caption={Using \code{if-else}.}]
%        {loop/conditional_in_loop_no_continue.java}
%    \end{minipage}\hfill
%    \begin{minipage}[t]{.45\textwidth}
%        \lstinputlisting[caption={Using \code{if..continue}.}]
%        {loop/conditional_in_loop_continue.java}
%    \end{minipage}
%\end{clisting}

% side-by-side two columns
%\begin{clisting}
%    \begin{minipage}[t]{.45\textwidth}
%        \lstinputlisting[caption={Unsimplified code}]
%        {loop/loop_cond_redundant_testing.java}
%    \end{minipage}\hfill
%    \begin{minipage}[t]{.45\textwidth}
%        \lstinputlisting[caption={Simplified code}]
%        {loop/loop_cond_repeated_block.java}
%    \end{minipage}
%\end{clisting}
%But each has their own problem.
%The left column do redundant condition testing (test1), whereas the right column has repeated
%code (\code{statement2}).

%The simplification of both lies in the use of \code{continue}:
%
%\begin{minipage}[H]{\textwidth}
%    \lstinputlisting{loop/loop_cond_no_redundancy.java}
%\end{minipage}


Similar situations may exist for the use of \code{break}, \code{return}, \textit{etc.} to reduce
redundant code.


\section{Refactoring loops}\label{sec:refactoringLoops}

Many loops, embedded in a long functions, are there to search for a match and should be
refactored into a function of its own.
Doing so not only simplifies the original function, making the main logic stand out more, but
also the factored out function is compact, single-purposed, and readily reusable.

Another reason is the elimination of many intermediate, local variables.
Intermediate variables in a long function often share the same scope as the function
and thus contaminate the namespace for the rest of the code block.
Eliminating them will prevent many mysterious bugs and spare developers from such concerns.

The following example illustrates this.
In the first version, the code in the main function \code{execute} was
misappropriated---a large part of it was usurped for the search of a needle in a haystack in a loop.
\lstinputlisting[lastline=12]{loop/refactor_conditional_in_loop.java}
After refactoring related code the loop into a separate function, we greatly reduced the bulge.
Most importantly, \code{target} and other intermediate variables, if any, may be eliminated and
will never pollute the namespace in the remaining part of the function.


\section{C-style For-loop}\label{sec:for-loop}

Beside the universal \code{while}-loop, another pre-test loop is the conventional
C-like \code{for}-loop
\footnote{This is not to be confused with the iterator-based foreach-loop such as
\lstinputlisting[basicstyle=\footnotesize]{loop/foreach-loop.java}
whose functionalities has been partly replaced by stream API as in \java.}
which looks like
\lstinputlisting{loop/for_loop.java}
The distinguishing parts of a \code{for}-loop are its header statements---the
initialization statement \code{<init>}, the loop continuation test
\code{<test>}, and the afterthought statement \code{<aftrth}.
Below is an explanation of each part:
\begin{enumerate}
    \item The \code{<init>} is a statement to be executed only once as control is transferred to
    the loop.
    \item The \code{<test>} may be regarded as the \emph{first} statement of the loop body.
    \item The \code{<test>} are executed one more time than the rest of loop body
    unless loop execution is aborted with, say, \code{break}.
    \item The \code{<aftrtht>} is the afterthought statement executed at the end of each
    iteration of loop body even in the presence of \code{continue}.
    But in the presence of \code{break} statement, \code{<aftrtht>} will be skipped together with
    part of the loop body.
    \item All three header statements are optional.
    Namely, `\code{for(;;);'} is perfectly legal and represents an infinite loop.
\end{enumerate}


Design considerations for a \code{for}-loop are summarized below.
\begin{itemize}
    \item
    What is the correct and minimal initialization?
    For example, should we initialize \code{int i = -1} or \code{int i = 0}?
    \item
    Where to update loop state?
    For example, where should \code{i++} or \code{j--} be placed?
    in the afterthought of the \code{for} header statement or somewhere in the loop body?
    \item
    What's the termination condition(s) how to arrange terminating statements if there are more
    than one of them?
\end{itemize}

The answers to these questions are interrelated and
shall be adjusted as a whole throughout the design and implementation of loops.
For example it is more flexible (sometimes necessary when certain condition
has to be met to increment/decrement)
to increment/decrement state variables in loop body but it is seldom the more concise form.
In a more concise loop, state updates are often done in the afterthought header statement.
For another, infinite loop is often the result of a reasoning loophole,
which causes incorrect termination condition.


The single biggest reason for me to use a \code{for}-loop is its limited scope of
loop variables.
Scope of loop variables declared in the \code{<init>} part of header statements are contained
within the loop.
For example the following code wouldn't compile
\begin{lstlisting}
    for (int i = 0; i < 10; ++i) {}
    System.out.println(i); // i is undefined
\end{lstlisting}
In contrast, loop variables used by the \code{while}-loop condition have to be declared
before the loop.
Therefore their scope last beyond the loop and this often cause problems for the subsequent
program for the aforementioned reasons.


Occasionally limited scope of \code{for}-loop may be a hinder for post-loop processing.
If that is the case, one may resort to
`one-more-loop' technique (see ~\autoref{subsec:one-more-loop} for more details) to solve the
problem.
Also for \python users, \code{for-else} or \code{while-else} is designed just for post-processing.


\section{The `\textit{loop-and-a-half}' problem}\label{sec:loop-and-a-half-Problem}

As discussed above, when it comes to programming with loops, there are a variety of constructs
such as pre-test loop and post-test loop.
Have you wondered why we need so many varieties and families?
Is there a one-size-fit-all loop?


To answer this question, let's dig into history a bit.
In 1966, Corrado B\"{o}hm and Giuseppe Jacopini proved that \code{goto} statements are
unnecessary and programs containing them can be transformed to use only conditional statements
and loops\cite{Bohm:1966}.
Along that line of thought, more researchers, who might have gone too far, attempted to remove
other `nuisance' in computer programming such as \code{break} and \code{return} in the middle of a
loop to reduce control flow complexity and promote code composability.
But the benefit cannot pay for the price.
For example, one result of doing so is that more local variables are needed.
Even worse, it exacerbates problems with duplicate code in and around a loop.
This is the origin of the so-called ``Loop and a half problem''\cite{Louden-Lambert-2011}.
Today, ``\textit{loop-and-a-half} problem'' is still a pestering problem for suboptimal loop
design and implementation.


At the language level, a number of loop constructs have been introduced to
minimize the symptoms of the ``\textit{loop-and-a-half} problem''.
But that's not enough.
Tt remains on developers to choose the right loop constructs.
Further, finer level code tuning is beyond simple choices of loop constructs
and must be hand-crafted at implementation time.
In the section to follow, we will introduce a coding technique for loop optimization in this aspect.


\section{Loop Rotation}\label{sec:LoopDesignConsideration}

It is beneficial to visualize a loop construct as a roulette, a carousel, or a Ferris wheel.
Mathematically speaking, there is a shared property among all four---circular sequences,
a ring with certain rotational degrees of freedom.


If pictured as a Ferris wheel, various statements are arranged around a loop
(see ~\autoref{fig:loop-rotation}).
When cut the loop open, any one of them may be the `first' statement, apart from a
varying amount of code duplication.
This freedom results from the rotational degrees of freedom of the circular sequence.
Conversely, at design time, one can take advantage of this rotational degrees of freedom to
fine tune the loop implementation by `rotating' the loop to match up and annex code at afinity
of the loop.
\begin{figure}[htb]
    \centering
    \includegraphics[width=.7\textwidth]{loop/ferris-wheel.jpg}
    \caption{\label{fig:loop-rotation}Loop as a circular sequence.
    The nodes \code{i++} and \code{print} are candidates that can matched and annexed into
    the loop.}
\end{figure}


Another helpful pictorial image is to think of loops as spools and programs as threads---
loops are introduced to help organize threads.
Thus code that is duplicate from a loop body is like thread that can be wound up into a spool
but is hanging around loosely.
Therefore, when using a loop in a program, try to spool up slack threads as much as possible
by `rotating the loop' to match and eliminate the duplicate part of the code.
\begin{figure}[htb]
    \centering
    \begin{subfigure}{.45\textwidth}
        \includegraphics[width=\textwidth]{loop/loose-spool.jpg}
        \caption{\label{fig:loose-spool} Loop with loosely coupled, surrounding code is like a
        loose spool.}
    \end{subfigure}
    \begin{subfigure}{.45\textwidth}
        \includegraphics[width=\textwidth]{loop/tight-spool.jpg}
        \caption{\label{fig:tight-spool}A crafted loop is like a neatly tightened spool, free
        from loose threads.}
    \end{subfigure}
    \caption{\label{fig:spool-loop}Craftsmanship in loop programming is analogous to organizing
    threads using a spool.}
\end{figure}
Thanks to the rotational degree of freedom, one can `rotate' the loop to match and spool up the
duplicate initialization code or post-loop processing logic into the loop.
This often requires developers to look for more general mathematical forms of the
algorithm used.
%TODO
The main motivation for doing so is to minimize code duplication and
this is one of the most important goal when designing a loop.


In practice, one may first implement a non-discriminating loop,
such as \code{while (true)} or \code{for (; ;)}, for prototyping purposes.
Once that code is working, one often has gotten a good grasp of the problem and the solutions.
Then one can start rotating the loop by picking the entrance and exit ports to spool up code into
the loop as much as possible.
In the end, one can try different loop constructs, moving and fitting
state-updating statements, combining or annihilating \code{break} or
\code{continue} statements, \textit{etc.}
If needed, repeat the above steps until the code is acceptable both in its functionality and from
aesthetic point of view.


\lstinputlisting[]{loop/word_breaker.py}


\section{Loop Initialization}\label{sec:loopInitialization}

What is the simple boundary conditions to be used for initialization?
In light of the above discussion of `loop rotations', we can reconsider this first tasks in the
design of loops.

It may not be so straightforward because it really depends on the specific problem.
But more often than not, the initialization part of a loop is simple, effortless,
intuitive values---integer $0$, empty list, zero-length string, array of simple values,
\textit{etc}.
As an example, ~\autoref{lst:fibonacci} illustrates the initialization of the Fibonacci sequence
with its first two elements: $0$ and $1$.
\lstinputlisting[label={lst:fibonacci},
caption={A cached solution for the $n$th Fibonacci numbers.}]
{loop/fibonacci.py}
Note that we have `back-filled' the Fibonacci numbers to the $0$th element as $0$.
You will see more cases later where some generalization of the algorithm and/or the mathematical
concept behind will make the coding much easier.
This effort of simplifying initialization process is especially important for dynamic programming
where often the boundary conditions are
usually an array of simple values.
See ~\autoref{ch:dp} for more information.
If you find that your initialization code actually repeats the loop body in whole or in part,
or that the post-processing logic does similar things that
the loop does,
That's a sign to `tighten' your loop, to absorb the pre-loop and post-loop logic
into the loop.
what can you do to get rid of them?
In the next couple of sections, we are going to present tips that can help you alleviate
symptoms associated with `loop-and-a-half' problem.


\section{`One-more-cycle' coding technique}\label{subsec:one-more-loop}

Sometimes after loop is completed, non-trivial post-processing is needed to transform the output
to final answer and that happy to share part of the logic of the loop body.
To reduce duplication of code, one can use the `one-more-cycle' technique---
instead of
\begin{lstlisting}
    for (int i = 0; i < list.size(); ++i)
\end{lstlisting}
and then repeat part of the loop body in post-loop processing logic,
do
\begin{lstlisting}
    for (int i = 0; i < 1 + list.size(); ++i)
\end{lstlisting}
and with an \code{if}-branching, hitchhiking part of the loop body to do the processing
for the last iteration and return the final result.
\@\autoref{lst:largest-rectangle-histogram} is an example of such usage for solving the
`largest rectangle in histogram' problem
\@\footnote{\url{https://leetcode.com/problems/largest-rectangle-in-histogram}}.

\lstinputlisting[label={lst:largest-rectangle-histogram},
caption={Maximum area under histogram}]
{loop/histogram.java}
To see the benefit of using the `one-more-cycle' technique, try iterate \code{h.length} times and
do the rest in post-loop logic and testify yourself for the duplicate code between loop
logic and post-loop processing.


\section{loop pacing}\label{sec:linearVs.NonlinearStepping}
Deterministic linear stepping \textit{vs.} nonlinear or conditional stepping.

If viewed as a state machine automaton, loop variables, serving as state registers, may change
linearly as loop is executed, or not.
In some cases, one can simply step through linearly, one element at a time.
In others, one need to step through with a varying pace conditioned by the elements encountered,
and this is called `nonlinear stepping' in the following context.
Nonlinear stepping has important applications in implementation of complex algorithms.
\code{while}-loop is more accommodating for all kinds of stepping.
For \code{for}-loop, if you see the afterthought part missing, that's probably a sign for
nonlinear stepping.
If you use \code{for}-loop for linear stepping,
please try your best to afterthought part for increment or decrement purposes.

\lstinputlisting[]{loop/array_jumper.java}

In some other cases, loop state may even jumps back and forth.
This is an extreme case of nonlinear stepping and is often used in solving math problems.
Consider this coding question:
\begin{quote}
    ``Given an array of integers where each element is the index of an element in the array.
    How would you detect if there is a cycle in this array?
    A cycle is a circular path with at least 2 nodes.
    ''
\end{quote}
\lstinputlisting[label={lst:nonlinear-stepping},
caption={Nonlinear stepping in a loop}]{loop/nonlinear_stepping_example.java}
%TODO finish the presentation of GOOGLE problem

This is a specialized graph problem in nature.
It is specialized because there are exactly one outgoing edge from each node.
Because of this specialty, the normal graph algorithms such as DFS or BFS would be an overkill.


\section{Conclusion}

The best practice when it comes to programming with loops, as any other types of coding,
is to get a \emph{working version} (WV).
Once you get a WV, you can spot for areas of improvement:
duplicate code of any severity, excessive customization such as \code{break} or \code{continue}
\textit{etc}.
Try the techniques we talked about in this chapter such as loop rotation, include
`one-more-loop',
try on different loop constructs and so on.
Also keep in mind opportunities of using sentinels for certain niched applications (see
~\autoref{ch:sentinels}).
Finally, narrow down to a loop construct
that is a close match to your programming requirement
minimal loop customization, if needed at all.
The ultimate goal is to minimize or eliminate duplicate code in and around loop.

\lstinputlisting[]{loop/NumberOfPath.java}
\lstinputlisting[]{loop/WildcardStringMatching.java}
