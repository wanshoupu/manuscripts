%%
%% Author: shoupu_wan
%% 7/14/18
%%

\chapter{Organize your Conditional Statements}\label{ch:organizeYourIfElse}

Conditional statement is a switch of control flow to alter code execution path at runtime,
conditioned by one or more boolean conditions.
This is a basic feature of almost all programming languages.
It may be strange that I am actually spending a full chapter on this as
many books and articles have already given this comprehensive discussions,
\textit{e.g.},
Wikipedia\footnote{\url{https://en.wikipedia.org/wiki/Conditional_(computer_programming)}}.

But just because it is basic and widely discussed does not mean that many people can do it right
or bother their willpower to do it right.
Seldom the first version of implementation of complicated logic is the optimal solution.
But based on my observation working in top-tier tech companies,
the very first version of intertwined logic often makes its way to production code that is
depended by millions.

``As long as it works, why should one bother to simplify the logic?'', you may argue.
I'd like to quote Steve McConnell to respond: ``The primary subject of communication is not computer
but other humans''\cite{Mcconnell.2004}.
Intertwined logic, even though it works, are hard to read, understand, debug, and modify.
It fails the primary purpose of communicating to other humans.
Worse, it often results in redundant/duplicate code.

Twisted logic in code often reveals a fierce battle that the author struggles to make something
that works.

I believe every minute spent with the basics is worthwhile before making publish pull requests on
production code.

%Communicate clear, succinct code to colleagues and successors are much more efficient.

In this chapter, I'm going to share my experience of how to polish complicated logic into simple,
beautiful, and succinct code.
out \code{if elseif else} switching statements to reduce redundancy condition checks on the one
hand and duplicate code on the other.

Some of the techniques I employ is of math origin, especially basic Boolean algebra such as logical
equivalences, normal forms, \textit{etc.}
Through plenty of examples, I demonstrate how to apply these techniques for logic simplification.
In doing so, I hope programmers would be inspired to the beautiful, succinct conditional statements.

\section{\code{if-else}-condition}\label{sec:if-else-condition}

A boolean variable has two values-\code{true} and \code{false}.
A simple condition in a \code{if-else} test has two execution alternatives-the \code{if}-block or
the \code{else}-block;
A composite condition of two boolean variables and a logical connective, such as
\code{or}/\code{and}, may have up to 4 alternatives;
and so on.

But when disguised in real problems, especially when some alternatives are omitted or no-op,
optimal coding may not be so straightforward.

\lstinputlisting[label={lst:embeddedBranching},
caption={Code with embedded branching and redudant
statements.}]{if-else/composite_condition_tobe_simplified.java}

Take a look at \autoref{lst:embeddedBranching}.
This code snippet has the following ``smell''\cite{Fowler1999}:
\begin{itemize}
    \item Complicated conditional statements
    \item Duplicate code
\end{itemize}
As a result,
it is hard to figure out what condition would lead to each execution path without a mental twist.
%This logic can be simplified if one observe that out of the total $4$ combinations of the two
%conditions, ...
This is a real implementation of my own.
% TODO
\begin{table}[h]
    \begin{center}
        \caption{Normal forms for conditions of execution of each statement.
        \label{tbl:breakdown-of-conditions}}
        \begin{tabular}{|l|l|}
            \hline
            code & condition to execute \\
            \hline
            statement1 & condition1 $AND$ condition2 \\
            statement2 & condition1 $AND$ !condition2 $OR$ !condition1 \\
            statement3 & condition1 \\
            \hline
        \end{tabular}
    \end{center}
\end{table}

\begin{table}[h]
    \begin{center}
        \caption{True value table for \code{condition1} and \code{condition2}.
        \label{tbl:true-value-table}}
        \begin{tabular}{|c|c|c|}
            \hline
            \code{condition1} & \code{condition2} & execution \\
            \hline
            \code{false} & \code{false} & \code{statement2}\\
            \code{false} & \code{true} & \\
            \code{true} & \code{false} & \code{statement2}, \code{statement3}\\
            \code{true} & \code{true} & \code{statement1}, \code{statement3}\\
            \hline
        \end{tabular}
    \end{center}
\end{table}


Then I spent time to eliminated the embedded conditional statement and the resultant code
is shown in\autoref{lst:simpleBranching}.
Note that the first condition was negated so that the \code{if\{..\}else if\{..\}else\{\}} can be
sequentially built on top of one another.
But this implementation still contains `duplication code' smell.
It is still not the simplest.
So let's find a piece of draft paper and write down the breakdown of conditions for the execution
of each statement as shown in \autoref{tbl:breakdown-of-conditions}.
Obviously the key to simplify the logic is to simplify the condition for \code{statement2}.

\begin{align}
    \label{eq:boolean-expression-simplify}
    c &= a \land \lnot b \lor \lnot a \nonumber \\
    &= a \land \lnot b \lor \lnot a \land (b \lor \lnot b) \nonumber \\
    &= a \land \lnot b \lor \lnot a \land b \lor \lnot a \land \lnot b
\end{align}
\lstinputlisting[label={lst:simpleBranching},
caption={Equivalent code snippet as \autoref{lst:embeddedBranching} with simple condition checking
}]{if-else/composite_condition_simplified1.java}

Use of logical equivalences: double negation elimination, De Morgan's laws, distributive law,
and normal forms such as disjunctive normal form (DNF) or conjunctive normal form (CNF), one can
simplify the logic conditions to achieve the most simplified \code{if-else} blocks.

One can eliminate duplicate code altogether as shown in \autoref{}
\lstinputlisting{if-else/composite_condition_simplified2.java}


\section{Branching in a loop}\label{sec:branchingInLoop}

\begin{table}[h]
    \begin{center}
        \caption{Equivalent code side-by-side.
        On the left, is unsimplified code.
        On the right, is simplified code.
        \label{tbl:equivalent-code}
        }
        \begin{tabular}{p{7cm}|p{7cm}}
            Option 1 & Option 2  \\
            \lstinputlisting{if-else/redundant_testing.java}
            &
            \lstinputlisting{if-else/repeated_block.java} \\
        \end{tabular}
    \end{center}
\end{table}

Shown in \autoref{tbl:equivalent-code} side-by-side are equivalent code snippets.

But each has their own problem.
The left column do redundant condition testing (condition1), whereas the right column has repeated
code (statement2).

Let's think of the logic in terms of a real-world example.
Suppose you need to check schedule and weather before making a plan.
\label{Evernte:if-else-if-else vs. continue in a loop}
\begin{itemize}
    \item Condition C1: schedule permits
    \item Condition C2: weather permits
\end{itemize}

\begin{itemize}
    \item Action A1: Meet a friend
    \item Action A2: Have a walk
    \item Action A3: Call a friend
\end{itemize}

The conditions for action A1 is \code{C1 == True && C2 == True};
The conditions for action A2 is \code{C1 == True};
The conditions for action A3 is \code{C1 == False && C2 == False}.

The simplification of both lies in the smart use of continue:
\lstinputlisting{if-else/no_redundancy.java}

The following linked list operation code are equivalent between the left and the right
\begin{table}[b]
    \begin{center}
        \caption{An example of a table.}
        \begin{tabular}{p{7cm}|p{7cm}}
            Option 1 & Option 2  \\
            \lstinputlisting{if-else/linked_list_operation1.java}
            &
            \lstinputlisting{if-else/linked_list_operation2.java} \\
        \end{tabular}
    \end{center}
\end{table}

The following linked list operation code are equivalent between the left and the right
\begin{table}[b]
    \begin{center}
        \caption{An example of a table.}
        \begin{tabular}{p{7cm}|p{7cm}}
            Option 1 & Option 2  \\
            \lstinputlisting{if-else/linked_list_operation3.java}
            &
            \lstinputlisting{if-else/linked_list_operation4.java} \\
        \end{tabular}
    \end{center}
\end{table}

The following generic code on the left and right are equivalent
\begin{table}[b]
    \begin{center}
        \caption{An example of a table.}
        \begin{tabular}{p{7cm}|p{7cm}}
            Option 1 & Option 2  \\
            \lstinputlisting{if-else/generic_conditioning1.java}
            &
            \lstinputlisting{if-else/generic_conditioning2.java} \\
        \end{tabular}
    \end{center}
\end{table}

This illustrates the main difference between \code{if-else-if-else} and multiple-\code{if}.
Firstly note that exactly one of the statements in a \code{if-else-if-else} block is executed
(or no more than one when there is no final else statement).
Secondly, note that \code{if-else-if-else} and multiple-\code{if} can be rewritten with one
another.
This is the foundation of simplifying a block if statements.

\section{Build up your case step-by-step}

The correct way to design the implementation of recursive, case-based testing code implementation is to do testing / checking such that knowledge is built up piece-by-piece.
To inspect for logic holes and false assumptions, do it backward: from the very end go to the start, find out if the knowledge needed to perform the current line of code is in place. Any missing knowledge will result in exception.

Case 1: override the equals function for a class

\lstinputlisting{if-else/equals.java}

Case 2: determining the lowest common ancestor of two values in a BST:

\lstinputlisting{if-else/lca.java}

Often there are so many cases to test, if not designed properly, your code can blow out like crazy.
