%%
%% Author: shoupu_wan
%% 7/14/18
%%
\chapter{Reusable Functions}\label{ch:reusableFunctions}
Before we get to the meat of this chapter, I feel obligated to touch base on functions.
When one starts implementing a class, \textit{e.g.}  one that implements an interface, one is
responsible to fulfill the requirements of the interface, \textit{i.e.}, implement all the public
methods listed in the interface.

I've seen too many times that developers, my former self included, treat each public method as
isolated island and implement them from head to toe in one monolithic block as the following:

\lstinputlisting[language=Java]{functions/foo.java}

``What else can we do when the interface contains remotely related methods?'' you may being
protesting by now.
You are right, methods in an interface may `share-nothing'.
So let's back off a little bit and suppose we are talking about an interface with totally
unrelated methods.
In this case, we can still do something to factor out the complicated conditions and flow control.
Firstly, apparently the main flow consists of two blocks-the \code{while}-loop and the
\code{if-else} block and each does a separate check of its own.
So let's refactor them into two private functions.
Of course we should adopt a meaning name for each of them.
But since everything here is for illustration, we will give them \code{barForIllustration} and
\code{ForIllustration}

Note that for simplicity, I refer to both standalone functions as well as member methods of a
class as \emph{functions}.

There are more things to improve in this illustrative block of code.
I will leave the explanation of how cleaner flow of controls to \fullref{ch:organizeYourIfElse}.

\section{Create easy to reuse modules or functions}

\label{Evernote:Rotate array by k times}
\label{Evernote:Search for start and end of a target in a sorted array}

\section{Why function?}

It is the same answer we would give to the question "Why design?", that is, to support abstract
coding.
Our brains are small.
So we have to reduce the complexity of code to understand the symantics.
Abstract the implementation with the name of a function enables us to do works with more complexity.
More importantly, think of coding as building pyramids.
Firstly we make the building blocks-bricks, carve stones, etc..
Then we build structures with these blocks.
Finally, we assemble structures into pyramid.
Higher level constructions depends on lower level constructions.

\section{Case Study--Spiral Matrix} \label{sec:case-spiral-matrix}

\label{Evernote:Refactor process example - Spiral Matrix}

\section{Spiral printing of matrix elements}

Spiral Printing of an 2D matrix. See EverNote

\section{Modularity}

Just breaking up a 100-line of function into five 20-line function does not create modularity.
That only makes it worse.

Here are some tips about you should break out a chunk of code into a separate function
\begin{itemize}
    \item In retrospective, understanding the code you just typed is beyond a glimpse.
    \item You are embedding a subtask $B$ within the body of your main function
    $A$.
    \item Obtaining certain information is so lengthy that it obstructs the main flow of control.
    \item A squad of certain lines are needed in more than two places, near or far, in your codebase.
    \item A sequence of operations appears again and once more.
    \item You encounter difficulty to create effective tests for a function.
\end{itemize}

These are low-hanging fruits of signals