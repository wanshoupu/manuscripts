# def binary_search(array, target, slope):
pivot = arr[m]
if pivot < target:
    if m < ridge:
        start = m + 1
    else:
        end = m
elif val > target:
    if m < ridge:
        end = m
    else:
        start = m + 1
else:
    return m