%%
%% Author: shoupu_wan
%% 7/14/18
%%

\chapter{Loops} \label{ch:loops}

All major programming languages feature one or more loop types as control flow and
it is one of the most widely used building block.
Loop is a programing productivity amplifier---with a static loop body and
a scarce of overhead code (\textit{e.g.}, loop variables), the loop may be executed as many
times as needed.
But many developers often haphazardly stumble upon a first-working loop and settle.
Seldom anyone would try to think about the difference between the varieties of loops,
even fewer people would try different loop structures and loop types and see
which best fits the current situation.
This is so not because these developers have no appetite for better code.
A majority of them are not even aware that conscious choice of loop types and structures can make a
marked difference in the quality of code.
Therefore, I dedicate this chapter as a primer to the topic ``How to close your loop?''
and I meant it literally.

``Why!'' you say, ``What's the point for you to talk about loops as a language feature
outside of the language context?''
Yes, there are so much to say about loops in their generic sense.
Similar to the structure in ~\fullref{ch:organizeYourIfElse}, I will share mostly my first-hand
coding experience throughout the presentation of the chapter.
Beyond that, this chapter is also presented for completeness since
later discussions of the interactions between conditional statements and loop is built on
top of reader's mastery of loop programming.
But most importantly, I intend this chapter to serve as an inspiration to other enterprising
programmers to reflect on why they code the way they do and find out ways to improve.

\section{Loop anatomy}\label{sec:loopAnatomy}

The location of conditional statements is critical to its correctness and readability.
Based on the location of the loop condition checking statement, loops can be roughly
grouped into: \emph{pre-test} loop, \emph{post-test} loop, \textit{etc.}

This section is an overview of loop structures in a few programming languages of choice for
bookkeeping purposes.
\code{for}-loop and \code{while}-loop are the most common pre-test looping flow of control that
almost all languages support.
\code{do-while}-loop is a post-test loop supported by \java, \cpp, C, and
others (but not \python).
We begin with the generic structure of loops.
Although there are some variations, the basic constructs of a loop are the same.
\@\autoref{tbl:loop-construct} describes the generic parts of a loop in high-level languages.
\begin{table}[h]
    \begin{center}
        \caption{Function anatomy\label{tbl:loop-construct}}
        \begin{tabular}{l|p{10cm}}
            \hline
            construct & description \\
            \hline
            Pre-loop initialization &
            This is the preparation section in \code{for}-loop where variables declared there are
            usually hidden from subsequent code\footnote{This is is true for \java, \cpp, C,
            \textit{etc.}, but is not true for, \textit{e.g.}, bash}.
            Note that \code{while}-loop does not have a similar construct. \\
            Continuation condition &
            The condition for control to enter (or continue) the loop. \\
            Body &
            The block of statements to be executed in the loop. \\
            \code{break} statement &
            A statement in loop body that breaks the loop and pass control to the next statement
            following the loop. \\
            \code{continue} statement &
            A statement in loop body that interrupt the execution of the remaining statements in the loop.
            Note that this doesn't break the loop or pass control out of the loop. \\
            Post-loop processing &
            This is a rare construct in loop, supported by languages such as \python as
            \code{while-else} and \code{for-else}.
            This part will not be discussed in the rest of this chapter. \\
            \hline
        \end{tabular}
    \end{center}
\end{table}
Depending on the situation, a loop may not have all the parts mentioned above.
For programmer's convenience, programming languages frequently supports one or more types of
loops.
Depending on the entry point into the loop circular sequence and the programming language
of choice,
some loop construct is often more convenient than others.

On top of the major offering of loop constructs, certain customization methods may be available
depending on the programming language.
For example, the termination condition checking is blended with the loop body with one or more
statement of \code{break} or \code{return}.
`\code{continue}' statement may be used in the middle of loop body so that certain part of the
loop may be skipped under certain circumstances.
\code{break}, \code{return}, and/or \code{continue} along with loop constructs of
programming language of choice fulfills majority of programming requirements (see
~\autoref{fig:fine-tune-loop-rotation}). %TODO make figure

\subsection{A word on the usage of \code{continue} and \code{break}}
\label{subsec:continue-and-break}

I would like to add a word on the usage of \code{continue} and \code{break} before going on with
other topics.
Bare use of \code{continue} and \code{break} in a loop is very rare (maybe only for educational
reasons).
Use of \code{continue} and \code{break} in a loop is usually done within a \code{if}-branch.

For most of the simple cases, the functionality realized by using \code{continue} statement can be
equivalently produced by adding an \code{else}-clause.
But when there is a `case stack' in a loop, use of \code{continue} has a magical power
of logic simplification.
For example, in ~\autoref{lst:loop-use-continue}, we code for a game for 2 or more players.
All players take turn to enumerate through natural numbers starting from $1$ with the following
rules:
\begin{enumerate}
    \item When the number is divisible by $2$, do a `clap' instead
    \item Otherwise when the number is divisible by $3$, do a `stump' instead
    \item Otherwise when the number is divisible by $5$, skip it altogether
    \item Otherwise articulate the number loudly
\end{enumerate}
For book keeping purposes, all numbers are recorded in a list unless skipped.

\begin{minipage}{\linewidth}
    \lstinputlisting[label={lst:loop-use-continue},
    caption={Example usage of \code{continue} to simplify the logic in a loop}]
    {loop/use_of_continue.py}
\end{minipage}

Without use of \code{continue}, it would be inevitable to have some duplicate code---one would
either repeat the conditions accumulated up to the point of divisibility by $5$
or repeatedly adding the \code{result.append(i)} in more than one blocks.

Similar situations exist for the use of \code{break} to reduce redundant code.
We are not going to repeat that with examples.

\subsection{C-style For-loop}\label{subsec:for-loop}

Beside the universal \code{while}-loop, another pre-test loop is the conventional
C-like \code{for}-loop as \lstinputlisting{loop/for_loop.java}
This is not to be confused with the iterator-based \code{for}-loop such as
\begin{lstlisting}
    for (Object obj : list)
\end{lstlisting}
which has been largely made obsolete with stream-like coding styles in \java.

There are some interesting aspects about \code{for}-loop header statements.
\begin{enumerate}
    \item The \code{<init>} is a statement to be executed only once as control is transferred to
    the loop.
    \item The \code{<condition>} may be regarded as the \emph{first} statement of the loop body.
    \item The \code{<condition>} are executed one more time than the rest of loop body
    unless loop is terminated with a \code{break} statement.
    \item The \code{<aftrtht>} is the afterthought statement executed at the end of each
    round, unless loop terminates with a \code{break} statement.
    \item The \code{<aftrtht>} is always executed as many times as the rest of loop body
    even in the presence of \code{continue}.
    \item All three header statements are optional.
    Namely, you may legally code `\code{for(;;);'} for infinite loop.
\end{enumerate}


Design considerations for a \code{for}-loop are summarized below.
\begin{itemize}
    \item
    Preparation code for a loop is called initialization
    and it determines the starting point of the loop.
    What's the best initialization?
    Should it be \code{int i = -1} or \code{int i = 0}?
    \item
    When and where should state updates?
    For example, where should \code{i++} or \code{j--} be placed?
    in the 3rd part of the \code{for} statement?
    or in the body?
    It's more flexible (sometimes necessary when certain condition
    has to be met to increment/decrement)
    if the increment/decrement is in the body.
    But it's more concise if done in the afterthought of a for-statement.
    \item
    What's the termination condition and How many termination points?
    Many a time, infinite loop is resulted when incorrect assumption is made
    about the termination condition.
\end{itemize}

In my opinion, the most desirable feature of a \code{for}-loop is its containment---
scope of loop-variables are contained within the loop.
For example the following code wouldn't compile
\begin{lstlisting}
    for (int i = 0; i < 10; ++i) {}
    System.out.println(i); // i is undefined
\end{lstlisting}
In contrast, loop variables used by the \code{while}-loop condition have to be declared
outside of the loop and extends beyond thereafter.
Sometimes this causes inadvertent namespace pollution that leads to treacherous bugs.

In practice, I often
start with \code{while (true)} or \code{for (; ;)} for prototyping and as a reminder to
myself that this piece of code is temporary.
Once that is working, I often have gotten a good grasp of the problem and the solutions.
Then I will start crafting the loop by trying different loop constructs, moving and fitting
variable increment statements,
combining or annihilating \code{break} or \code{continue} statements \textit{etc.}

These words may sound blurry and abstract to you.
So let us use examples to illustrate the ideas.
Feel free to pause and give them a thought yourself before reading my suggested approaches.

Say, a bank allows customers to invest in stocks with their bank accounts.
The rule is that one is allowed to take unlimited `\code{invest()}' actions
if and only if one's balance is greater than zero.
Each action of investment may result in gain or loss which is applied back to the account.
Then for a particular account, the program would look like
\lstinputlisting{loop/pre_test_loop.java}
To ensure the enforcement of policy, pre-test loop is used so that
each investment action is subject to a balance check before execution.
If the rule is changed such that one is allowed to invest if and only if his balance is > zero
\emph{or if the customer doesn't have investment history (first time investor)}.
We would refactor the above code as
\lstinputlisting{loop/pre_test_loop1.java}
In either case, the pre-test loop is the best fit.

For another example, suppose we are to develop a command line tool that takes input from
\code{stdin}.
The tool interprets user input and does a bunch of cool stuff.
It terminates when user types `exit' or simply hits `return' key
(in which case the input string is empty).
Then the best loop construct for that would be mid-test loop.
\lstinputlisting{loop/mid_test_loop.java}

For yet another example, in a cooking competition game, each chef is to cook 3-dish
meal---appetizer, main, and desert---with the following rules:
\begin{enumerate}
    \item He or she first makes one as a display dish.
    \item she then goes on to make same dishes for each referee who orders her meal for judgement
    until all orders are finished.
\end{enumerate}
This program is best coded with \code{do-while} loop as
\lstinputlisting{loop/post_test_loop.java}

% TODO illustrate use of different loop constructs with chicken-and-egg problem.
% Given a chicken or an egg and the functions chicken hatch(egg), egg lay(chicken)
% while(eggs) {
%   hatch()
%   if (has_hen()) {
%        lay()
%   }
% }
%
% do {
%   hatch()
% } while(eggs);
%

\section{The `\textit{loop-and-a-half}' problem}\label{sec:loop-and-a-half-Problem}

As discussed above, there are a variety of loop constructs and loop families such as
pre-test loop and post-test loop.
Have you wondered why we need so many varieties and families?
``Is there a one-size-fit-all loop?'' you ask.
To answer this question, we need to start with a research dated back 1966.
In 1966, Corrado B\"{o}hm and Giuseppe Jacopini proved that \code{goto} statements are
unnecessary and programs containing them can be transformed to use only conditional statements
and loops\cite{Bohm:1966}.
Along that line of thought, more researchers, who might have gone too far, attempted to remove
other `nuances' in computer programming such as \code{break} and \code{return} in the middle of a
loop to reduce control flow complexity and promote code composability.
But the price of doing so may not pay the benefit it brings.
More local variables are needed.
Even worse, it may require duplicate code before or after a loop.
This is the so-called ``Loop and a half problem''\cite{Louden-Lambert-2011}.

So in disguise, the \textit{loop-and-a-half} problem is code duplication problem
caused by unnecessarily restricting immature termination within a loop.
Variety of loop types are introduced to minimize the symptoms of the \textit{loop-and-a-half}
problem.

What is the simple boundary conditions to be used for initialization?
It may not be so straightforward because it really depends on the specific problem.
But more often than not, the initialization part of a loop is simple, effortless,
intuitive values---integer $0$, empty list, zero-length string, array of simple values,
\textit{etc}.
As an example, ~\autoref{lst:fibonacci} illustrates the initialization of the Fibonacci sequence
with its first two elements: $0$ and $1$.
\lstinputlisting[label={lst:fibonacci},
caption={A cached solution for the $n$th Fibonacci numbers.}]
{loop/fibonacci.py}
Note that we have `back-filled' the Fibonacci numbers to the $0$th element as $0$.
You will see more cases later where some generalization of the algorithm and/or the mathematical
concept behind will make the coding much easier.
This effort of simplifying initialization process is especially important for dynamic programming
where often the boundary conditions are
usually an array of simple values.
See ~\autoref{ch:dp} for more information.
If you find that your initialization code actually repeats the loop body in whole or in part,
or that the post-processing logic does similar things that
the loop does,
That's a sign to `tighten' your loop, to absorb the pre-loop and post-loop logic
into the loop.
what can you do to get rid of them?
In the next couple of sections, we are going to present tips that can help you alleviate
symptoms associated with `loop-and-a-half' problem.

\section{Loop Rotation}\label{sec:LoopDesignConsideration}

It is beneficial to visualize a loop construct as a roulette, a carousel, or a Ferris wheel, all
three of which shares the properties of a circular sequence---an sequence with rotational
degree of freedom.
Even after everything has been laid out onboard, there are still rotations as degrees of
freedom as shown in ~\autoref{fig:loop-rotation}.
Thus, one of the task in
loop design is to arrange loop logic in circular sequence and pick the best
entrance and termination ports.
\begin{figure}[htb]
    \centering
    \includegraphics[width=.7\textwidth]{loop/ferris-wheel.jpg}
    \caption{Loop as a circular sequence.\label{fig:loop-rotation}}
\end{figure}

Additionally, one can
think of a program as a linear thread and a loop as a spool that helps organize loose thread.
\begin{figure}[htb]
    \centering
    \begin{subfigure}{.45\textwidth}
        \includegraphics[width=\textwidth]{loop/loose-spool.jpg}
        \caption{\label{fig:loose-spool} Loop with loosely coupled, surrounding code is like a
        loose spool.}
    \end{subfigure}
    \begin{subfigure}{.45\textwidth}
        \includegraphics[width=\textwidth]{loop/tight-spool.jpg}
        \caption{\label{fig:tight-spool}A crafted loop is like a neatly tightened spool, free
        from loose threads.}
    \end{subfigure}
    \caption{\label{fig:spool-loop}Craftsmanship in loop programming is analogous to organizing
    threads using a spool.}
\end{figure}
Thanks to the rotation degree of freedom, one can `rotate' the loop to match and spool up the
duplicate initialization code or post-loop processing logic into the loop.
This often requires developers to look for more general mathematical forms of the
algorithm used.

\section{How to choose the right loop for concise program?}\label{sec:chooseTheRightLoop}

\subsection{\code{for} \textit{vs.} \code{while} loops}\label{subsec:for-vs-while}

I like for-loop for the limited scope of its loop-variables.
But sometimes limited scope may be a hinder for post-processing with \code{for}-loop.
I will often balance out the trade-offs and if I choose \code{for}-loop, I will use
`one-more-loop' technique (see ~\autoref{subsec:one-more-loop} for more details) to beat
post-processing outside of the loop.

\subsection{`One-more-cycle' coding technique}\label{subsec:one-more-loop}

Sometimes after loop is completed, non-trivial post-processing is needed to transform the output
to final answer and that happy to share part of the logic of the loop body.
To reduce duplication of code, one can use the `one-more-cycle' technique---
instead of
\begin{lstlisting}
    for (int i = 0; i < list.size(); ++i)
\end{lstlisting}
and then repeat part of the loop body in post-loop processing logic,
do
\begin{lstlisting}
    for (int i = 0; i < 1 + list.size(); ++i)
\end{lstlisting}
and with an \code{if}-branching, hitchhiking part of the loop body to do the processing
for the last iteration and return the final result.
@\autoref{lst:largest-rectangle-histogram} is an example of such usage for solving the
`largest rectangle in histogram' problem
\@\footnote{\url{https://leetcode.com/problems/largest-rectangle-in-histogram}}.

\lstinputlisting[label={lst:largest-rectangle-histogram},
caption={Example usage of \code{continue} to simplify the logic in a loop}]
{loop/histogram.java}
To see the benefit of using the `one-more-cycle' technique, try iterate \code{h.length} times and
do the rest in post-loop logic and testify yourself for the duplicate code between loop
logic and post-loop processing.


\subsection{Linear stepping \textit{vs.} nonlinear stepping}\label{subsec:linearVs.NonlinearStepping}

If viewed as a state machine automaton, loop variables, serving as state registers, may change
linearly as loop is executed, or not.
In some cases, one can simply step through linearly, one element at a time.
In others, one need to step through with a varying pace conditioned by the elements encountered,
and this is called `nonlinear stepping' in the following context.
Nonlinear stepping has important applications in implementation of complex algorithms.
\code{while}-loop is more accommodating for all kinds of stepping.
For \code{for}-loop, if you see the afterthought part missing, that's probably a sign for
nonlinear stepping.
If you use \code{for}-loop for linear stepping,
please try your best to afterthought part for increment or decrement purposes.

In some other cases, loop state may even jumps back and forth.
This is an extreme case of nonlinear stepping and is often used in solving math problems.
Consider the infamous Google\texttrademark interview prep question
\begin{quote}
    ``Given an array of integers where each
    element points to the index of the next element how would you detect if there is a cycle in this
    array''\footnote{\url{https://www.geeksforgeeks.org/check-loop-array-according-given-constraints}}.
    \subsection{Sentinels to avoid condition checking}\label{subsec:sentinelsAvoidConditionChecking}
\end{quote}
\lstinputlisting[label={lst:nonlinear-stepping},
caption={Nonlinear stepping in a loop}]{loop/nonlinear_stepping_example.java}
%TODO finish the presentation of GOOGLE problem

\subsection{\code{do-while} loop to execute first loop without checking
condition}\label{subsec:do-while-loop}

\code{do-while} loop attempts to execute the loop body at least once no matter what because it
only checks the condition at the end of one round of loop execution.
This is useful sometimes to combine an initialization section with loop body.
Choosing pre-test and post-test loops is like choosing restaurants that charge
first versus ones that serve first.
If you are sure you you have enough money and you need a meal no matter what, you can go to a
restaurant that serves first.
But if you want to make sure you have enough money to pay for what you eat before you do it, you
should go to one that charges first.

%TODO find one case that best use do-while
A case for \code{do-while} loop is as follows.


\subsection{Use of \code{continue} to simplify conditional
statements}\label{subsec:continue-in-loop}


\subsection{Conclusion}

The best practice when it comes to programming with loops, as any other types of coding,
is to get a \emph{working version} (WV).
Personally, I like to start with \code{while (true)} or \code{for (; ;)} for prototyping.
Once I get a WV, I've formed a pretty good idea for the implementation
Then inspect the WV for pre-loop initialization and post-loop processing.
If you can spot duplicate code of any severity, it may be a sign for loop rotation.
Experiment with loop rotation starting with the WV:
try on different loop constructs, move the condition
and afterthought statements around, \textit{etc}.
Also remember the use of sentinels may also be a good option (see ~\autoref{ch:sentinels}).
The purpose of this is to minimize or eliminate duplicate code in the pre-loop initialization and
post-loop processing.
Finally, narrow down to a loop construct
that is a close match to your programming requirement
minimal loop customization, if needed at all.


\begin{itemize}
    \item Firstly use a generic (infinite loop) with break statements as exit points
    \item in the body such as for(;;) or while(true). After the structural
    \item statements are clear, one can choose a suitable, concise loop statements
\end{itemize}

\lstinputlisting[]{loop/NumberOfPath.java}
\lstinputlisting[]{loop/iterative_graph_traversal.java}
\lstinputlisting[]{loop/array_jumper.java}
\lstinputlisting[]{loop/WildcardStringMatching.java}
