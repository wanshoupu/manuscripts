def tokenize(string):
    ...
    while indx < len(string):
        if string[indx] == '\\':
            indx += 2
            continue
        if quoted: # TODO need to simplify the logic
        ...
