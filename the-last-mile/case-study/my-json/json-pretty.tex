%%
%% Author: shoupu_wan
%% 8/22/18
%%

\chapter{Json prettifier}\label{ch:json-prettifier}

The problem of designing a Json prettifier is a project that is close to production working
environment.
At first sight, the problem may seem to be a coding problem.
But after careful examination, much of the complicated logic encountered in
the problem actually can be designed away.
The problem is thus a good practice candidate for person with intermediate software development
goals.
In this chapter, we are going to go through the various strategies in attacking the
problem.
We are going to evaluate the trade-offs with explanations of why certain choices are preferrable
that may have long-term benefit that's are hidden from the current situation.

After going through this practice, I hope that reader may get strengthened on their critical
thinking skills as well as basic OOD application skills are required.

\section{The problem statement}\label{sec:theProblemStatement.Json}

The requirement for the Json prettifier can be simply as ``Given a string as a Json blob, format
it into pretty, properly indented string''.
Json is a data serialization format.
Primitive Json element types include string, number, true, false, and null.
Structural Json element types are dictionary and list and the keys for dictionary
must be of type string\footnote{See Json spec at \url{https://www.json.org/}}.

But complication may come from invalid Json strings, where validation may be a prerequisite
before formatting and prettifying.
Other complexities may come from escape characters, including quotes like single/double quotes.

\section{Outline of the algorithm}\label{sec:outlineOfTheAlgorithm.Json}

The algorithm consists of two steps.
Step one, parse Json data;
Step two, reformat the Json data by adding newlines, indent spaces, \textit{etc.}

\section{Implementation}\label{sec:implementation.Json}

This is another example where complexity management involves separating the complexities,
then divide and conquer strategy is used, and the complexities are handled in
different modules.
The input string may be a blob of strings with no newlines or spaces in between.
Or the input may be `almost' well-formatted Json files, off by just a few spaces or
newlines---this requires minimal `style-compliance' formatting.
Or the input may be invalid Json, in which case the formatting will be interrupted
by a report of errors found.

This inspires us to validate and parse the input string into an in-memory data structure
first and then print the structure as pretty string.
So supposedly, the prettifying code would look like

Once we are clear of the structure of the solution, we can code up the top-level solution and we
are way ahead of monolithic implementations (see ~\autoref{lst:JsonPrettifier}).
\lstinputlisting[label={lst:JsonPrettifier},
caption={Json prettifier code that invokes the modular constructs of \code{JsonParser} and
\code{JosnFormatter} to prettify input Json blob data.}]
{case-study/my-json/JsonPrettifier.py}

Next we need to implement two functions \code{parseJson(json_str)} and \code{toPrettyString(obj)}.
~\autoref{lst:JsonFormatter.py}, and ~\autoref{lst:JsonParser.py}.

\subsection{Json Parser}\label{subsec:jsonParser}

\lstinputlisting[label={lst:JsonParser.py}
caption={Json formatter.
It take an in-memory object reprensetation of Json data, print it into well-formatted string.}]
{case-study/my-json/JsonParser.py}

\subsection{Json formatter}\label{subsec:jsonFormatter}

An initial implementation
\lstinputlisting[label={lst:JsonFormatter.py}
caption={Json formatter.
It take an in-memory object reprensetation of Json data, print it into well-formatted string.}]
{case-study/my-json/JsonFormatter.py}

But that is one possible implementation.

Let us revisit the json formatter code since I believe there are still something
we can learn from.
As you may notice there is a function \code{def __white__(indent)} where \code{indent} is an
integer that provides the global indent levels for the line.
This function is called in multiple places
to provide the correct indentation for individual lines.

But there is another way of indenting structures.
~\autoref{lst:new-indentation-code}
\lstinputlisting[label={lst:JsonParser.py}
caption={A new implementation of Json formatter.}]
{case-study/my-json/NewJsonFormatter.py}
The new implementation does not use a function such as \code{def __white__(indent)} to do the
indentation.
Instead, it jack up the indentation for all lines in a block that represents a data structure.

