%%
%% Author: shoupu_wan
%% 7/14/18
%%

\chapter{Organize your Conditional Statements}\label{ch:organizeYourIfElse}

Conditional is a control switching mechanism to alter code execution path,
conditioned by boolean expressions.
As a control flow, conditional is one of the fundamental building blocks
of almost all programming languages.
Undoubtedly, many references---textbooks, research articles, blog posts---have already covered this
comprehensive discussions
\footnote{\url{https://en.wikipedia.org/wiki/Conditional_(computer_programming)}}.
It may seem strange that I am spending a chapter on such a `well-known' topic here and now.
However, just because the topic is basic and `well-known' does not mean that many folks can do it
right or will do it right.
But based on my years of working experience in several top-tier tech companies, it is often
the haphazard first working version that makes its way to production codebase and becomes the
core for millions of businesses, it does not matter how crooked the design may be, how
obscure the logic may be.
%But it is seldom the case that the first version of implementation of any non-trivial logic, if
%correct at all, is optimal.
%It fails the primary purpose of communication with other humans.
%Worse, it often results in redundant/duplicate code.
``As long as it works, why should one bother to simplify the logic?'', you may argue.
As a response, I'd like to quote Steve McConnell:
``..the primary audience [of your code] is made up of humans, not computers.''\cite{Mcconnell.2004}.
\begin{quote}
    ``Any fool can write code that a computer can understand.
    Good programmers write code that humans can understand.''\\
    —Martin Fowler
\end{quote}

Twisted code and intertwined logic are often unsimplified
products out of fierce battle fought by a struggling developer.
Though hard to read, understand, debug, and modify, they should not be regarded as enemy.
On the opposite, they are the dawn of simple, effective implementation
and the rest of this chapter is dedicated to show you how.
%Communicate clear, succinct code to colleagues and successors are much more efficient.
I'm going to share my experience of how to polish complicated logic involving conditionals into
simple, beautiful, and succinct code.
You will learn how to organize conditional statements with \code{if}, \code{else
if} (or\code{elif}), \code{else}, \textit{etc.},
to produce effective, succinct logic.
You would minimize occurrences of redundancy condition checks in your code and reduce
duplicate code overall.
Every minute you spend on grasping the basic conditional programming is worthwhile before
plunging into other topics.
Through plenty of examples, I demonstrate how to apply these techniques for logic simplification.
In doing so, I hope programmers would be inspired to the beautiful, succinct conditional statements.

\section{Basic techniques for conditional statement simplification}
\label{sec:conditional-simplify-tech}

Some of the techniques I employ in this section is of math origin, especially basic Boolean algebra
such as logical equivalences:
double negation elimination, De Morgan's laws, distributive law, \textit{etc.}
and normal forms: disjunctive normal form (DNF) or conjunctive normal form (CNF).
Review and refresh your memory on relevant materials as you see fit.

A boolean variable or expression can evaluate to two possible values-\code{true} and \code{false}.
Depending on the evaluation result of a boolean value,
a \code{if-else} test has two execution alternatives-the \code{if}-block or
the \code{else}-block;
A composite condition consisting of two boolean values may have up to 4 alternatives.
Three boolean values make have up to 8 alternatives.
And so on.
\begin{figure}[htb]
    \centering
    \begin{subfigure}[b]{.4\textwidth}
        \includegraphics[width=\textwidth]{if-else/conditional.jpg}
        \caption{Conditional}
    \end{subfigure}
    \begin{subfigure}[b]{.4\textwidth}
        \includegraphics[width=\textwidth]{if-else/nested-conditionals.jpg}
        \caption{$3$ conditionals}
    \end{subfigure}
    \caption{\label{fig:conditional-branching}Branching effect of conditional statements.}
\end{figure}

But when embedded in real problems, disguised by specific forms, especially when some alternatives
are masked, achieving optimal conditional programming may not be so straightforward.
Let us examine the code in ~\autoref{lst:nestedBranching} (this is my own code at earlier years
of my software development career).
% side-by-side two columns
\noindent\begin{minipage}[t]{.3\textwidth}
             \lstinputlisting[label={lst:nestedBranching},
             caption={v1}]
             {if-else/composite_condition_tobe_simplified.java}
\end{minipage}\hfill
\noindent\begin{minipage}[t]{.3\textwidth}
             \lstinputlisting[label={lst:simpleBranching},
             caption={v2}]
             {if-else/composite_condition_simplified1.java}
\end{minipage}\hfill
\noindent\begin{minipage}[t]{.3\textwidth}
             \lstinputlisting[label={lst:final-simplified-conditional-statement},
             caption={v3}]
             {if-else/composite_condition_simplified2.java}
\end{minipage}


It is the very first version after I figured out the requirement from a problem and,
as expected, it 'smells'---it has complicated, nested conditional statements and duplicate
code\cite{Fowler1999}.
Because of these problems, one can hardly figure out which statement is executed under what
condition without a mental twist.
\begin{table}[bth]
    \begin{center}
        \caption{Normal forms for conditions of execution of each statement.
        \label{tbl:breakdown-of-conditions}}
        \begin{tabular}{|l|l|}
            \hline
            code & condition to execute \\
            \hline
            statement1 & test1 $\land$ test2 \\
            statement2 & test1 $\land\lnot$ test2 $\lor\lnot$ test1 \\
            statement3 & test1 \\
            \hline
        \end{tabular}
    \end{center}
\end{table}
Nevertheless, this is a good starting point for further improvement.
What's important is to keep on working---do not settle on half-cooked solution.

So upon arriving at this first solution, I decided as a next step to eliminate the nested
conditional statements.
Observed that the \code{else} block of \code{if (test1)} is much simpler, I decided to flip
the logic by negating \code{test1}.
Then I promoted the nested \code{if (test2)} up to the first-level so that overall they
become sequential conditional statements comprised of \code{if\{..\}else if\{..\}else\{\}} blocks
that are built on top of one another.
This turns out to be a coding pattern that I'm going to spend more time to explain
in ~\autoref{sec:case-stack}.
The resulting code is as shown in ~\autoref{lst:simpleBranching}.

Now the conditional statements are streamlined, straightforward and are easily understood at a
glance.
Comparing with ~\autoref{lst:nestedBranching}, it is a big improvement.
But still it has duplication code (both \code{statement2} and \code{statement3} are repeated).
So I did a back-of-envelop calculation on a napkin---for each of the three statements
(which consists of the conditional block body),
I wrote down the conditions for the corresponding block to be executed.
The result is shown in ~\autoref{tbl:breakdown-of-conditions}.
With this table, it is clear that the key to simplify the logic lies in the conditions for the
execution of \code{statement2}.
Let us do some Boolean algebra.
Let $c$ be the condition for \code{statement2} to be executed and
\begin{align*}
    a &= \text{\code{test1}}, \\
    b &= \text{\code{test2}}.
\end{align*}
Then
\begin{align}
    c &= a \land \lnot b \lor \lnot a  \nonumber \\
    &= a \land \lnot b \lor (\lnot a \land \lnot b \lor \lnot a \nonumber) & \text{// absorption law
    }\\
    &= (a \land \lnot b \lor \lnot a \land \lnot b) \lor \lnot a \nonumber & \text{// associativity
    law}\\
    &= \lnot b \lor \lnot a & \text{// distributivity law} \nonumber \\
    &=\lnot (a \land b) & \text{// De Morgan's law} \label{eq:boolean-expression-simplify}
\end{align}

~\autoref{eq:boolean-expression-simplify} shows that the condition of execution for
\code{statement2} is \code{!(test1 && test2)} and this can be piggybacked as the
\code{else}-block of
of the conditional construct for \code{statement1}, which is \code{if (test1 && test2)}.
With this hard-earned wit, it now becomes straightforward to come up with yet an even simpler
implementation such as ~\autoref{lst:final-simplified-conditional-statement}.

Please note that the above process is not the only way to arrive at simplified logic.
Another possible way to achieve same logic simplicity is to use `truth table'.
Let's see how this works.
\begin{table}[h]
    \begin{center}
        \caption{True value table for \code{test1} and \code{test2}.
        \label{tbl:true-value-table}}
        \begin{tabular}{|c|c|c|}
            \hline
            \code{test1} & \code{test2} & true expr \& execution\\
            \hline
            \code{false} & \code{false} & \code{!test1} $\rightarrow$ \code{statement2} \\
            \code{false} & \code{true} & \code{!test1} $\rightarrow$ \code{statement2} \\
            \code{true} & \code{false} & \code{test1 \&\& !test2}
            $\rightarrow$ \code{statement2} \\
            & & \code{test1=true} $\rightarrow$ \code{statement3} \\
            \code{true} & \code{true} & \code{test1 \&\& test2} $\rightarrow$
            \code{statement1} \\
            & & \code{test1} $\rightarrow$ \code{statement3} \\
            \hline
        \end{tabular}
    \end{center}
\end{table}
I have constructed truth table as ~\autoref{tbl:true-value-table}.
Since there are two conditional variables \code{test1} and \code{test2}, there are
total four entries in the truth table.
The third column shows the boolean expression that's evaluated to \code{true} by which
the execution of the statement ensues.
Summarizing the table entries, we arrive at the following conclusions
\begin{enumerate}[label={\roman*:}]
    \item \code{statement1} is executed iff both \code{true};
    \item \code{statement2} is executed iff not both \code{true};
    \item \code{statement3} is executed iff \code{test1=true}, regardless of
    \code{test2}.
\end{enumerate}

With all the mental twists untied, implementation of something like
~\autoref{lst:final-simplified-conditional-statement} naturally follows.

In this section, we presented two methods for logic simplification.
One method en routes on the necessary condition for the execution of certain statements and seek to
simplify the conditional expressions.
The other considers all the possible branches and looks for ways to combining them.
In practice, you are encouraged to try out both ways and for your own instinct about the methods.
You may find one or the other process to be more beneficial under certain circumstances.

\section{Build up `case-stack'}\label{sec:case-stack}

As a programming language feature,
it is not unfamiliar to many a sequence of consecutive conditional statements
starting with \code{if} clause, followed by zero, one, or more of
\code{else if} blocks (\code{elif} for \python), and optionally with
\code{else} block, \textit{e.g.}, ~\autoref{lst:simpleBranching}.
For the sake of argument, we refer to such construct as `case-stack'.
Case-stack forms an effective pattern when coding fairly complicated logic.
Together with a loop construct, case-stack is an important pattern to implement a wide range of
algorithms. (Please refer to ~\autoref{ch:loops} for more discussion).

Unfortunately, little is mentioned about this patterns in programming references.
Beginning and intermediate developers often stumble upon this without concious choice, lest to
master the best practice of it.
In this section, so we hope to give it a fair coverage over some of the important aspects of a
case-stack and the best practices around it.

When faced with multi-branch switching,
a common way of implementation uses nested \code{if..else} statements such as the example given at
the beginning of this chapter ~\autoref{lst:nestedBranching}.
As switching fanout grows, the nesting level goes deeper, indentation grows messier, and
the logic quickly becomes unwieldy.
As an alternative for better readability, one can organize multi-branch switching using a
case-stack as ~\autoref{lst:simpleBranching}.
For some special cases, \code{switch}-statement may be used equally well, if supported by the
programming language.
For simplicity, we limit our discussion to \code{if..else}-statement here.

The execution model of a case-stack is like walking up a staircase---the program
successively evaluates the conditional expression one at a time and builds up the cases.
The conditional expression of the leading \code{if}-block is the first step of check.
If it is evaluated to \code{true}, then the \code{if}-block is executed
and the rest of the stack is skipped.
Otherwise, the next conditional expression is evaluated
and so on until the end of the case-stack.
Note that in the presence of \code{return} and \code{break},
the case-stack may be simplified with the removal of the \code{else} keyword.
For examples, take a look at ~\autoref{lst:if-stack-equals} and ~\autoref{lst:if-stack-lca}.

First property of a case-stack is the order of conditional statements which is often carefully
designed for succinct code, optimal performance or both.
The second property is the cumulative effect of successive tests.
We are going to talk these properties below.

\begin{table}[b]
    \begin{center}
        \caption{\label{tab:cumulative-tests-case-stack}Cumulative effect of tests in a case
        stack.}
        \begin{tabular}{p{7cm}|p{7cm}}
            Case stack & Equivalent decoupled conditional statements \\
            \lstinputlisting{if-else/case_stack_example.java}
            &
            \lstinputlisting{if-else/case_stack_taken_apart.java} \\
        \end{tabular}
    \end{center}
\end{table}
When taken apart, the effect of tests of a case-stack
is cumulative---a later case combines all previous conditional expressions up to its own.
This is illustrated by the example of ~\autoref{tab:cumulative-tests-case-stack}.
Note that unlike the strict ordering of the conditional statements in the left column,
the three \code{if}-blocks in the right column are not logically coupled and
thus may be swapped assuming that the execution of \code{statement1}, \code{statement2},
\code{statement3}, do not affect one another.

When debugging a case-stack for logic fallacies and unfounded assumptions, for example,
one needs to keep this cumulative effect in mind: all the assumptions about the
the current line of code should have been tested by an earlier conditional expression.
A missing test will likely result in exception in subsequent execution.
From this perspective, the right column in ~\autoref{tab:cumulative-tests-case-stack} would be a
good aid for debugging---due to its explicit conditional expressions, it is more
straightforward to spot untested assumptions made for a later case than its case-stack
counterpart.

\begin{figure}[htb]
    \lstinputlisting[label={lst:if-stack-equals},
    caption={Example implementation overriding \code{equals} in a subclass}]
    {if-else/equals.java}
\end{figure}
As a real-world example to demonstrate the effectiveness of case-stack in handling
multi-branching logic,
let's consider another example of case-stack implementation---the method
\code{public boolean equals(Object obj)} as shown in ~\autoref{lst:if-stack-equals}.
The first test we do is the nullity of the argument.
Only if it is not null do we move on to the second test---if it's of type \code{MyClass}.
Only if it is not null and is of type \code{MyClass} do we cast it to type \code{MyClass}
and perform pairwise equality tests between member variables of
\code{this} instance and \code{that} instance.
Once again, you see how the program progresses up a `staircases' step-by-step.

To illustrate how the order of tests can affect the effectiveness of a case-stack,
let's consider the problem of
determining the lowest common ancestor of two values in a binary search tree.
In this problem, the number of `special' cases is combinatorial.
If not designed properly, the code of handling these cases can blow out like crazy.
However, with a properly designed case-stack, a couple of lines of code can handle
the myriad possibilities like a breeze.
Take a look at ~\autoref{lst:if-stack-lca}.
The cases are built from ground up like this:
\begin{enumerate}
    \item Test nullity;
    \item Test for left branching;
    \item Test for right branching;
    \item Test for existence of keys;
    \item Final case;
\end{enumerate}
Note later statements are built on the assertions of previous tests---test for left/right
branching requires the non-null attributes of root otherwise the code will throw
\code{NullPointerException}.
Also note that as the cases are being built up, the solution space is also being divided and
half of it is ruled out much like that of a binary search.
\begin{figure}[htb]
    \lstinputlisting[label={lst:if-stack-lca},
    caption={An implementation of finding lowest common ancestor in a binary search tree.}]
    {if-else/lca.java}
\end{figure}
Some source states that when designing case-stack do the simple tests first.
I think that's a superfluous observation.
The underlying rule is to do the test consisting of most entropy first.
That way, the maximal solution space can be chopped off at the earliest possible time.
Generally speaking, building up the cases of an case-stack is ultimately empirical.
Even the experienced developers may have to make a couple of attempts to get the optimal design.

\section{Logic equivalence among conditional expressions}\label{sec:logicEquivalences}
%TODO
% side-by-side two columns
\noindent\begin{minipage}[t]{.45\textwidth}
             \lstinputlisting[caption={Unsimplified},firstline=1,lastline=4]
             {if-else/equivalent_statements_if_return.java}
\end{minipage}\hfill
\noindent\begin{minipage}[t]{.45\textwidth}
             \lstinputlisting[caption={Simplified},firstline=6,lastline=6]
             {if-else/equivalent_statements_if_return.java}
\end{minipage}

% side-by-side two columns
\noindent\begin{minipage}[t]{.45\textwidth}
             \lstinputlisting[caption={Unsimplified},firstline=1,lastline=3]
             {if-else/equivalent_statements_ternary_expr.java}
\end{minipage}\hfill
\noindent\begin{minipage}[t]{.45\textwidth}
             \lstinputlisting[caption={Simplified},firstline=5,lastline=7]
             {if-else/equivalent_statements_ternary_expr.java}
\end{minipage}

\section{Conditional statements in a loop}\label{sec:branchingInLoop}

The questions when it comes to thinking of the base cases are:
What if the matrix is a row array?
Column array?
What if the matrix is a one-cell matrix?
Extreme question: what if the matrix is empty?
Do you just throw exception?

\begin{figure}[htb]
    \lstinputlisting[label={lst:matrix-spiral-term},
    caption={Matrix spiral print termination position.}]
    {if-else/spiral_termination_position.py}
\end{figure}

% side-by-side two columns
\noindent\begin{minipage}[t]{.45\textwidth}
             \lstinputlisting[caption={Using \code{if-else}.}]
             {if-else/conditional_in_loop_no_continue.java}
\end{minipage}\hfill
\noindent\begin{minipage}[t]{.45\textwidth}
             \lstinputlisting[caption={Using \code{if..continue}.}]
             {if-else/conditional_in_loop_continue.java}
\end{minipage}

With the keywords \code{break} and \code{continue}, Logic simplification in a loop
therefore has one extra piece of complexity and a new weapon to wield.
Shown below side-by-side are equivalent code snippets.

% side-by-side two columns
\noindent\begin{minipage}[t]{.45\textwidth}
             \lstinputlisting[caption={Unsimplified code}]
             {if-else/loop_cond_redundant_testing.java}
\end{minipage}\hfill
\noindent\begin{minipage}[t]{.45\textwidth}
             \lstinputlisting[caption={Simplified code}]
             {if-else/loop_cond_repeated_block.java}
\end{minipage}
But each has their own problem.
The left column do redundant condition testing (test1), whereas the right column has repeated
code (statement2).

Let's think of the logic in terms of a real-world example.
Suppose you need to check schedule and weather before making a plan.
\label{Evernte:if-else-if-else vs. continue in a loop}
\begin{itemize}
    \item Condition C1: schedule permits
    \item Condition C2: weather permits
\end{itemize}

\begin{itemize}
    \item Action A1: Meet a friend
    \item Action A2: Have a walk
    \item Action A3: Call a friend
\end{itemize}

The conditions for action A1 is \code{C1 == True && C2 == True};
The conditions for action A2 is \code{C1 == True};
The conditions for action A3 is \code{C1 == False && C2 == False}.

%TODO
\lstinputlisting[caption={Equivalent code snippets}]{if-else/equivalent_statements.java}

The simplification of both lies in the smart use of continue:
\lstinputlisting{if-else/loop_cond_no_redundancy.java}

The following linked list operation code are equivalent between the left and the right
\begin{table}[b]
    \begin{center}
        \caption{An example of a table.}
        \begin{tabular}{p{7cm}|p{7cm}}
            Option 1 & Option 2  \\
            \lstinputlisting{if-else/linked_list_operation1.java}
            &
            \lstinputlisting{if-else/linked_list_operation2.java} \\
        \end{tabular}
    \end{center}
\end{table}

The following linked list operation code are equivalent between the left and the right
\begin{table}[b]
    \begin{center}
        \caption{An example of a table.}
        \begin{tabular}{p{7cm}|p{7cm}}
            Option 1 & Option 2  \\
            \lstinputlisting{if-else/linked_list_operation3.java}
            &
            \lstinputlisting{if-else/linked_list_operation4.java} \\
        \end{tabular}
    \end{center}
\end{table}

The following generic code on the left and right are equivalent
\begin{table}[b]
    \begin{center}
        \caption{An example of a table.}
        \begin{tabular}{p{7cm}|p{7cm}}
            Option 1 & Option 2  \\
            \lstinputlisting{if-else/generic_conditioning1.java}
            &
            \lstinputlisting{if-else/generic_conditioning2.java} \\
        \end{tabular}
    \end{center}
\end{table}

This illustrates the main difference between \code{if-else-if-else} and multiple-\code{if}.
Firstly note that exactly one of the statements in a \code{if-else-if-else} block is executed
(or no more than one when there is no final else statement).
Secondly, note that \code{if-else-if-else} and multiple-\code{if} can be rewritten with one
another.
This is the foundation of simplifying a block \code{if}-statements.

For a really good example of multi-way conditional statement in a loop,
the ~\fullref{lst:silhouette-skyline} presented in ~\fullref{ch:loop-thinning}
would make an excellent example.

