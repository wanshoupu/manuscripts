%%
%% Author: shoupu_wan
%% 7/14/18
%%

\chapter{Loops} \label{ch:loops}

All major programming languages features one or more loop as a type of control flow.
You can visualize a loop construct as a carousel or a Ferris wheel, with an
entrance, a number of turns and an exit.
Loop is a powerhouse, a productivity amplifier for programing-with a static loop body,
one or a few loop variable(s), and nominal overhead code, the loop may be executed as many
times as needed.

To developers, much of the coding work is spent on choosing the right loop construct, choosing
loop variable(s), and designing the loop body.

Construction wise, loop is like an embedded machine inside computer programs-one prepares
input, feed it to the loop, and collect the output at the end.

\code{for}-loop and \code{while}-loop are the most common looping flow of control that almost all
languages support.
\code{do-while}-loop is a variation of \code{while}-loop that's supported by \java, \cpp, C, and
others but not \python.

``Why!'', you say, ``What makes you talking about loops as a language feature with no
language context?''
While loop is the one of the most basic building block for coding, beginners and intermediate
developers often haphazardly stumble on one loop and found that it works for them and settle.
Seldom anyone thinks about the difference between the varieties of loops.
Especially, many are unaware of the fact that conscious choice of loop types can make a marked
difference in the readability and conciseness your code.

As in in \fullref{ch:organizeYourIfElse}, I am sharing my first-hand coding experiences which
address this beginner's problems.
Also later discussions about the interplay between conditional statements and loop is built on
top of a good understanding of the various types of loops.
So this chapter is also presented for completeness.

But most importantly, I intend this chapter to serve as an inspiration to other enterprising
programmers to reflect on why they code the way they do and find out ways to improve.

\section{Loop anatomy}\label{sec:loopAnatomy}
Although there are some variations, the basic constructs of a loop are the same.
\autoref{tbl:loop-construct} describes the parts of a loop in high-level languages.

\begin{table}[h]
    \begin{center}
        \caption{Function anatomy\label{tbl:loop-construct}}
        \begin{tabular}{l|p{10cm}}
            \hline
            construct & description \\
            \hline
            Pre-loop initialization &
            This is the preparation section in \code{for}-loop where variables declared there are
            usually hidden from subsequent code\footnote{This is is true for \java, \cpp, C,
            \textit{etc.}, but is not true for, \textit{e.g.}, bash}.
            Note that \code{while}-loop does not have a similar construct. \\
            Continuation condition &
            The condition for control to enter (or continue) the loop. \\
            Body &
            The block of statements to be executed in the loop. \\
            \code{break} statement &
            A statement in loop body that breaks the loop and pass control to the next statement
            following the loop. \\
            \code{continue} statement &
            A statement in loop body that interrupt the execution of the remaining statements in the loop.
            Note that this doesn't break the loop or pass control out of the loop. \\
            Post-loop processing &
            This is a rare construct in loop, supported by languages such as \python as
            \code{while-else} and \code{for-else}.
            This part will not be discussed in the rest of this chapter. \\
            \hline
        \end{tabular}
    \end{center}
\end{table}

Depending on the situation, a loop may or may not have certain parts as mentioned above.

\section{Loop classification}\label{sec:loopClassification}

As mentioned at beginning of this chapter, a loop may be viewed as a `Lazy Susan' turntable.
Depending on the location of the \code{<condition>} statement, there are three possible classes:
pre-condition loop, mid-condition loop, and post-condition loop.

\subsection{Pre-condition loop}\label{subsec:pre-conditionLoop}

Pre-condition loop is the simplest loop and amongst them, \code{while} loop is the simplest (see
\autoref{lst:while-loop}).

\begin{minipage}{\linewidth}
    \lstinputlisting[label={lst:while-loop},
    caption={Pre-condition while loop structure.}]{loop/while_loop.java}
\end{minipage}

Another form of pre-condition loop is the conventional C-like \code{for}-loop as in
\autoref{lst:for-loop}.

\begin{minipage}{\linewidth}
    \lstinputlisting[label={lst:for-loop},
    caption={For loop structure as a pre-condition.}]{loop/for_loop.java}
\end{minipage}

There are some interesting aspects about for-loop.
The \code{<init>} is a statement to be executed only once before control enters the loop.
The \code{<condition>} may be regarded as the \emph{first} statement of loop body.
The \code{<condition>} are always executed one more time than the rest of loop body
unless there are break or continue statements inside.
The \code{<post-stmt>} is the \emph{last} statement of for-loop body.
and
The \code{<post-stmt>} is always executed as many times as the rest of loop body
unless there are break or continue statements inside.

I like for-loop for the limited scope of its loop-variables.
But I like to start with \code{while ( true )} or \code{for (; ; )} for prototyping.
Once I get a better idea for the implementation, I will settle down to the best fit.


\subsection{Mid-condition loop}\label{subsec:mid-conditionLoop}

There is rarely any construct that directly support mid-condition loop \textit{per se}.
Combining a \code{while (true)} with a conditional \code{break} is the closest approximation of a
mid-condition loop (\autoref{lst:mid-condition-loop}).

\begin{minipage}{\linewidth}
    \lstinputlisting[label={lst:mid-condition-loop},
    caption={Mid-condition loop structure.}]{loop/mid_condition_loop.java}
\end{minipage}

\subsection{Post-condition loop}\label{subsec:post-conditionLoop}

The \code{do-while} loop featured in C, \cpp, or \java, but not \python, is a good example of
post-condition loop (\autoref{lst:post-condition-loop}).

\begin{minipage}{\linewidth}
    \lstinputlisting[label={lst:post-condition-loop},
    caption={Post-condition loop structure.}]{loop/post_condition_loop.java}
    This is equivalent to
    \lstinputlisting{loop/post_condition_loop_equivalent.java}
    albeit duplicate code.
\end{minipage}

\section{The Loop-and-a-half problem}\label{sec:Loop-and-a-halfProblem}

``Why do we need so these varieties of loops?'' you say.

In 1966, Corrado B\"{o}hm and Giuseppe Jacopini proved that \code{goto} statements are
unnecessary and programs containing them can be transformed to use only conditional statements
and loops\cite{Bohm:1966}.
Along that line of thought, more researches which might have gone too far, attempted to remove
other `nuances' in computer programming such as \code{break} and \code{return} in the middle of a
loop to reduce control flow complexity and promote code composability.
But doing so are with prices.
More local variables are needed.
Even worse, it may require duplicate code before or after a loop.
This is the so-called ``Loop and a half problem''\cite{Louden-Lambert-2011}.

In summary, the loop-and-a-half problem is the problem of partial code duplication of a loop
body caused by unnecessarily restricting in-between termination of a loop.
In the following sections, we will study how to avoid loop-and-a-half problems with a few case
studies.

\begin{itemize}
    \item Pre-loop duplicate
    \item post-loop duplicate
\end{itemize}
\section{How to choose the right loop for concise code?}\label{sec:chooseTheRightLoop}

I like for-loop for the limited scope of its loop-variables.
But I like to start with \code{while ( true )} or \code{for (; ; )} for prototyping.
Once I get a better idea for the implementation, I will settle down to the best fit.

\begin{itemize}
    \item Firstly use a generic (infinite loop) with break statements as exit points
    \item in the body such as for(;;) or while(true). After the structural
    \item statements are clear, one can choose a suitable, concise loop statements
\end{itemize}

What are the important parts when using a for-loop?
Questions to ask and answer
\begin{itemize}
    \item
    what's the initialization?
    Initialization is the starting points. To iterate over an array, for example,
    should the initialization be i = -1 or i = 0?
    \item
    when and where increment/decrement should happen?
    in the 3rd part of the for statement? or in the body?
    It's more flexible (sometimes necessary when certain condition has to be met to increment/decrement)
    if the increment/decrement is in the body. But it's more concise to put in the for-statement
    \item
    What's the termination condition and How many termination points?
    Often, infinite loop is resulted when incorrect assumption is made about the termination condition
\end{itemize}
