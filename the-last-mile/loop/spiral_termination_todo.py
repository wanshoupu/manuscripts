def spiral_term(m, n):
    # TODO initialization to be determined
    i, j = _, _
    while 1 < m and 1 < n:
        i += 1
        j += 1
        m -= 2
        n -= 2
    # calculate offsets for special cases
    return i, j
